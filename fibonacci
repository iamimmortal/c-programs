#include<stdio.h>
int main()
{
	int n,i,f1=0,f2=1,f3;
	printf("Enter range for fibonacci series:");
	scanf("%d",&n);
	printf("\nFibonacci series are:");
	for(i=1;i<=n;i++)
	{
		f3=f1+f2;
		f1=f2;
		f2=f3;
		printf("%d\t",f3);
	}
	return 0;
}